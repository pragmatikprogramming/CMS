<nav>
    <ul class="nav">
        @foreach (var page in Model)
        {
            if(page.PageTitle != "Hidden Pages")
            {
                @:<li><a href="/@page.PageID">@page.NavigationName</a></li>
            }
        }
    </ul>
    <div class="clear"></div>
</nav>
